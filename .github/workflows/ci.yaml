name: myWorkflow
on:
  push:
    branches:
      - develop
jobs:
  build:
    runs-on:
      - ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'oracle'
          java-version: 24
      - name: Build with Gradle
        run: ./gradlew clean build