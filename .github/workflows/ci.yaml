name: myWorkflow
on:
  push:
    branches:
      - develop
jobs:
  build:
    runs-on:
      - ubuntu-latest
    steps:
      - uses: joschi/setup-jdk@v2.3.0
        with:
          java-version: 24
      - run: ./gradlew build